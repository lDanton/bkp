trim_method="trim"
max_len=20
roll_speed=2 

if [ "`mpc 2>&1 | wc -l`" -gt "1" ]; then
	if [ "`mpc | grep "^\[paused\]"`" != "" ]; then
	np="paused"
	    else
	np=`mpc current`
	    fi
	else
	np="nothing"
fi

echo "♫ ${np}" | cut -N 15
